extends layout

//- This file is kind of unused right now

block content
  .page-header
    h3 Chart.js

  canvas#myChart
  script(src='https://cdn.jsdelivr.net/npm/chart.js@2.9.3/dist/Chart.min.js')
  script.
    const x_labels = [];
    const weights = [];
    
    makeChart();
    async function makeChart() {
      const data = await getCSV(); //get CSV data first
      var ctx = document.getElementById('myChart').getContext('2d');
      var chart = new Chart(ctx, {
      // The type of chart we want to create
      type: 'line',
      // The data for our dataset
      data: {
      labels: data.x_vals,
      datasets: [{
      label: 'Weight',
      backgroundColor: 'rgb(255, 99, 132)',
      borderColor: 'rgb(255, 99, 132)',
      data: data.y_vals
      }]
      },
      //Config options
      options: {
        scales: {
            yAxes: [{
                ticks: {
                    callback: function(value, index, values) {
                        return value + ' kg';
                    }
                }
            }]
        }
      }

      });
    }

    //Read CSV data from file
    async function getCSV() {
        const x_vals = [];
        const y_vals = [];
        const response = await fetch("test.csv");
        const data = await response.text();
        console.log(data);

        const rows = data.split('\n').slice(1);
        rows.forEach(input => {
          const row = input.split(',');
          const id = row[0];
          const weight = row[3];
          const height = row[4];
          const bmi = row[5];
          x_vals.push(id); //For graph
          y_vals.push(weight);
          console.log(id, weight, height, bmi); //can treat id as 'date' on x-axis for now
        });
        return {x_vals, y_vals};
    }

  //- canvas#myChart(width='400' height='400')
  //- //-'./node_modules/chart.js'
  //- script(type='text/javascript', src='https://cdn.jsdelivr.net/npm/chart.js@2.9.3/dist/Chart.min.js').
  //-   //- var Chart = require('chart.js');
  //-   var ctx = document.getElementById('myChart').getContext('2d');
  //-   var myChart = new Chart(ctx, {
  //-   type: 'bar',
  //-   data: {
  //-   labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
  //-   datasets: [{
  //-   label: '# of Votes',
  //-   data: [12, 19, 3, 5, 2, 3],
  //-   backgroundColor: [
  //-   'rgba(255, 99, 132, 0.2)',
  //-   'rgba(54, 162, 235, 0.2)',
  //-   'rgba(255, 206, 86, 0.2)',
  //-   'rgba(75, 192, 192, 0.2)',
  //-   'rgba(153, 102, 255, 0.2)',
  //-   'rgba(255, 159, 64, 0.2)'
  //-   ],
  //-   borderColor: [
  //-   'rgba(255, 99, 132, 1)',
  //-   'rgba(54, 162, 235, 1)',
  //-   'rgba(255, 206, 86, 1)',
  //-   'rgba(75, 192, 192, 1)',
  //-   'rgba(153, 102, 255, 1)',
  //-   'rgba(255, 159, 64, 1)'
  //-   ],
  //-   borderWidth: 1
  //-   }]
  //-   },
  //-   options: {
  //-   scales: {
  //-   yAxes: [{
  //-   ticks: {
  //-   beginAtZero: true
  //-   }
  //-   }]
  //-   }
  //-   }
  //-   });